server:
  port: '9000'
  # logging:
  level:
    org:
      springframework:
        security: TRACE
spring:
  datasource:
    url: 'jdbc:mariadb://localhost:3306/springbootoauth2server'
    username: willy
    password: 'mO6/DHN[ry'
    driver-class-name: org.mariadb.jdbc.Driver

  flyway:
    enabled: true
    baseline-version: 0
    baseline-on-migrate: true
    schemas: springbootoauth2server
    table: schema_version
    validate-on-migrate: false

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB103Dialect
        format_sql: true
        default_schema: springbootoauth2server
    defer-datasource-initialization: false # https://stackoverflow.com/a/69235286 -> https://docs.spring.io/spring-boot/docs/current/reference/html/howto.html
    hibernate:
      ddl-auto: validate
    generate-ddl: false
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDB103Dialect
