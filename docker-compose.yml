version: '3.9'
volumes:
  data:
services:
  db:
    image: mariadb
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: springbootoauth2server
      MYSQL_USER: willy
      MYSQL_PASSWORD: 'mO6/DHN[ry'
    volumes:
      - data:/var/lib/mysql
    ports:
      - "3306:3306"