<?xml version="1.0" encoding="UTF-8"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         comparisonMethod="maven"
         xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0"
         xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0
         https://www.mojohaus.org/versions/versions-maven-plugin/xsd/rule-2.0.0.xsd">

    <!-- Global: ignore alpha, beta, milestone, release-candidate, and snapshot versions -->
    <ignoreVersions>
        <ignoreVersion type="regex">.*[-.]alpha[-.]?\d*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]beta[-.]?\d*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]Alpha[-.]?\d*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]Beta[-.]?\d*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]RC[-.]?\d*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]rc[-.]?\d*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]CR[-.]?\d*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]M\d+</ignoreVersion>
        <ignoreVersion type="regex">.*-SNAPSHOT</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]milestone[-.]?\d*</ignoreVersion>
    </ignoreVersions>
</ruleset>
